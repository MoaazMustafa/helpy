# Database layer

- `client.ts` opens the SQLite connection once and exports a typed `db`.
- `schema/*.ts` defines one Drizzle schema module per feature entity.
- `migrations/` is generated by Drizzle Kit.
- Repositories live next to the feature that owns them, under `src/features/<name>/services/*.repo.ts`. Components never import from here directly.
